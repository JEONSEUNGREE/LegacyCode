package com.example.demo.repository;

import com.example.demo.entity.SenarioMs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SenarioMsRepository extends JpaRepository<SenarioMs, Long> {
}
